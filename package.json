{
  "name": "pickup-service",
  "version": "1.6.96-staging",
  "private": true,
  "description": "픽업 서비스 - PostgreSQL 데이터베이스 사용",
  "author": "DOCORE",
  "license": "MIT",
  "scripts": {
    "build": "echo '✅ 빌드 완료 (현재 프로젝트는 빌드가 필요 없습니다)' && npm run lint:check",
    "lint:check": "node -c src/backend/api_server.js && echo '✅ JavaScript 문법 검사 통과'",
    "start:db": "docker-compose up -d postgres",
    "stop:db": "docker-compose down",
    "migrate:json-to-postgres": "node scripts/migrate-json-to-postgres.js",
    "start:api": "node src/backend/api_server.js",
    "start:prod": "NODE_ENV=production node src/backend/api_server.js",
    "dev:api:db": "npm run start:db && sleep 5 && npm run migrate:json-to-postgres && npm run start:api",
    "restart:api": "bash scripts/restart-api.sh",
    "test:api": "curl http://localhost:8081/api/healthz"
  },
  "dependencies": {
    "@google/generative-ai": "^0.5.0",
    "dotenv": "^16.3.1",
    "mime-types": "^2.1.35",
    "openai": "^6.7.0",
    "pg": "^8.11.3",
    "qrcode": "^1.5.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.23",
    "@types/pg": "^8.10.7",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "packageManager": "npm@10.2.0"
}
