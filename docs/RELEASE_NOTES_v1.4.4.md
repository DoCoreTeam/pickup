# Release Notes v1.4.4

## 📅 릴리즈 정보
- **버전**: v1.4.4  
- **코드명**: "Owner Control"  
- **릴리즈 날짜**: 2025-11-12  
- **타입**: Hotfix Update

---

## 🎯 주요 하이라이트

1. **점주 멀티 스토어 지원** – 여러 가게를 운용하는 사장님도 하나의 계정으로 가게 목록과 입점 신청을 관리
2. **도메인·QR 잠금 정책 강화** – 서브도메인 최초 설정, QR 생성 이후의 수정 권한을 Super Admin 전용으로 제한
3. **기본 정보 스텝 워크플로우 고도화** – 조회 중심 요약 + 단계별 수정 모달로 실수 없는 설정 흐름 제공

---

## ✨ 새로운 기능

- **가게 관리 대시보드 (Owner Mode)**
  - `/admin` 접속 시 점주 계정으로 로그인하면 `owner-mode` UI가 활성화됩니다.
  - 가게 목록은 `/api/stores?ownerId=` 기반으로 로드되며, 세션 캐시(`owner_store_list`)로 빠른 복원 지원.
  - “추가 입점 신청” 버튼을 통해 별도의 비밀번호 입력 없이 신규 가게 요청 가능 (`POST /api/owners/:ownerId/stores`).

- **기본 정보 스텝 모달 개선**
  - 요약 카드 → “설정 변경” 버튼 → 3단계 Wizard(기본 정보 → 연락처·주소 → 확인) 흐름으로만 수정하도록 고정.
  - Step 검증 시 기존 정규식(전화번호, 주소) 그대로 재사용.

---

## 🔐 권한 및 보안 변경

- **서브도메인 정책 강화**
  - 최초 등록 전: 점주도 입력 가능 (비어있는 경우)
  - 등록 후: 변경/삭제는 슈퍼어드민만 가능 (`role` 전달 필수)
  - QR 코드 생성 후(`qrLockedAt` 설정): 어떠한 계정도 수정·삭제 불가, Super Admin도 제외

- **QR 관리 제한**
  - QR 생성/업데이트/삭제는 슈퍼어드민만 수행 (UI/백엔드 동시 제한)
  - QR 코드 잠금 상태에서는 관련 버튼이 노출되지 않으며, 재생성·삭제 API도 400/403 반환
  - QR 다운로드는 PNG 확장자로만 제공 (`downloadQRCode`, `downloadDomainQR`)

- **점주 계정**
  - owner 로그인 응답에 `stores`, `primaryStoreId`, `storeCount` 포함
  - 세션 저장소에 `owner_store_list` 추가, 로그아웃/슈퍼어드민 로그인 시 정리
  - 비밀번호 변경은 여전히 점주 본인이 가능 (`POST /api/owners/:ownerId/password`)

---

## 🐛 버그 수정

1. 점주가 `stores.find` 오류로 선택된 가게를 표시하지 못하던 문제 수정
2. 도메인/QR 설정이 권한 없이 덮어쓰여 500/403을 유발하던 시나리오 차단
3. QR 업데이트 버튼이 점주 모드에서도 노출되던 UI 오류 수정

---

## ✅ 테스트

- **수동 시나리오**
  - 점주 로그인 → 복수 가게 조회/선택, 추가 입점 신청 모달 제출
  - 기본 정보 요약 → 스텝 모달 → 저장 후 요약/폼 반영 확인
  - 서브도메인 최초 등록 → 슈퍼어드민으로만 변경 가능 여부 검증
  - QR 생성 후 다운로드/업데이트/삭제 제한 동작 확인 (슈퍼어드민 vs 점주 구분)

---

## 🚀 배포 가이드

1. **API 서버 재시작**
    ```bash
    pkill -f "src/backend/api_server.js"
    cd /Users/dohyeonkim/작업용/pickup
    npm run start:api > server_runtime.log 2>&1 &
    ```
2. **브라우저 강제 새로고침**
   - `Cmd + Shift + R` (Mac) / `Ctrl + Shift + R` (Windows/Linux)
3. **태그 생성**
    ```bash
    git tag v1.4.4
    git push origin v1.4.4
    ```

---

**v1.4.4 릴리즈 완료!** 🎉




