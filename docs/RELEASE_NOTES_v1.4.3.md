# Release Notes v1.4.3

## 📅 릴리즈 정보
- **버전**: v1.4.3
- **코드명**: "Stability Anchor"
- **릴리즈 날짜**: 2025-11-12
- **타입**: Hotfix Update

---

## 🎯 주요 하이라이트

1. **스토어 설정 복구** – SEO·A/B 테스트 컬럼 및 히스토리 기능이 제한 권한 환경에서도 안전하게 동작
2. **관리자 대시보드 정상화** – 가게 목록/선택 로직 및 초기화 루틴 안정화로 로그인 직후 빈 화면 이슈 제거
3. **API 라우팅 보강** – SEO·A/B 테스트 히스토리 라우트가 쿼리 파라미터와 함께 동작하도록 개선

---

## ✨ 새로운 기능

- **권한 감지 기반 자동 폴백**
  - `store_settings` 테이블 ALTER 권한이 없으면 경고를 남기고 기본 데이터만 사용하도록 폴백
  - 히스토리 테이블 생성 권한이 없을 때 자동 비활성화하여 500 오류 방지
- **스토어 응답 정규화 헬퍼**
  - 관리자 프론트 `normalizeStoreListResponse`, `normalizeStoreDetailResponse` 추가로 API 응답 형태와 무관하게 안정적으로 동작

---

## 🔧 개선 사항

- SEO·A/B 테스트 관련 GET/POST API가 권한 부족(42501) 시에도 200 응답으로 빈 데이터 반환
- `/api/store/:id/(seo|ab)-test-settings/history` 라우트가 쿼리 문자열 유무와 관계없이 매칭되도록 수정
- 대시보드의 가게 선택/시각 표시, 토스트 메시지, 사용자 정보 로딩 플로우 전반을 재정렬하여 중복 호출 방지
- 저장 후 로컬 캐시 및 UI를 즉시 업데이트하도록 폼 저장 루틴 정비

---

## 🐛 버그 수정

1. 로그인 후 빈 페이지와 연쇄 `ReferenceError`가 발생하던 문제 해결
2. SEO·A/B 설정 API 호출 시 500(`42501`) 오류로 실패하던 문제 수정
3. 히스토리 요청이 쿼리 파라미터를 포함하면 404가 발생하던 문제 해결

---

## ✅ 테스트

- **수동 시나리오**
  - 슈퍼어드민 로그인 → 대시보드 로딩 확인
  - 가게 선택 및 정보 로딩 (기본 정보·배달앱·할인/픽업 섹션)
  - SEO/AB 설정, 히스토리 API 엔드포인트 `curl` 확인
  - QR 코드 생성 경로 정상 동작 확인

---

## 🚀 배포 가이드

1. **API 서버 재시작**
    ```bash
    pkill -f "src/backend/api_server.js"
    cd /Users/dohyeonkim/작업용/pickup
    npm run start:api > server_runtime.log 2>&1 &
    ```
2. **브라우저 강제 새로고침**
   - `Cmd + Shift + R` (Mac) 또는 `Ctrl + Shift + R` (Windows/Linux)

3. **태그 생성**
    ```bash
    git tag v1.4.3
    git push origin v1.4.3
    ```

---

**v1.4.3 릴리즈 완료!** 🎉





